{
  "name": "structura",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "clean": "turbo run clean"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0",
    "@biomejs/biome": "^1.9.0",
    "@types/node": "^22.0.0"
  },
  "packageManager": "npm@11.6.0"
}
